class ProductImage {
  constructor(imageId, productId, imageUrl, color) {
    this.imageId = imageId;
    this.productId = productId;
    this.imageUrl = imageUrl;
    this.color = color;
  }
}

module.exports = ProductImage;
